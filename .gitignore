.superpowers/
node_modules/
dist/
.dev.vars
.env
*.local
tsconfig.tsbuildinfo

# Playwright MCP scratch
.playwright-mcp/

# Local QA screenshots (root)
/*.png

# Cloudflare local dev state (local D1, logs)
.wrangler/

# D1 backup dumps — local-only plain JSON (public repo + users.email/password_hash).
# Never commit dumps. Do NOT re-add a GitHub Actions backup workflow/artifact:
# on a public repo, artifacts are downloadable by anyone signed into GitHub.
backup-out/
backups/local/

# Simulation run artifacts (paid API output, machine-specific)
sim-*.log
sim-*.json
